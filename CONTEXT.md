# Google Workspace MCP Server

AI アシスタントから Google Workspace（Sheets / Docs / Slides / Drive）を操作するための MCP サーバー。
1 人の利用者が会社用・個人用など複数の Google アカウントを使い分けることを前提にしている。

## Language

### アカウントまわり

**アカウント**:
利用者が使い分ける Google の identity。ラベルで識別し、メールアドレスは保持しない。
_Avoid_: ユーザー、プロファイル、テナント

**ラベル**:
利用者がアカウントに付ける論理名。ツール引数・ディレクトリ名・エラー文面に現れる唯一の識別子。
_Avoid_: アカウント名、ID、キー、エイリアス

**既定アカウント**:
ツール呼び出しで `account` が省略されたときに使われるアカウント。登録が 1 件だけならそれが自動的に既定になる。
_Avoid_: デフォルト、プライマリ、カレントアカウント

**OAuth クライアント**:
Google Cloud に登録したアプリケーション（`client_id` と `client_secret` の組）。アカウントと 1 対 1 とは限らず、
複数のアカウントで 1 つを共有することも、アカウント専用のものを使うこともある。
_Avoid_: 認証情報、クレデンシャル、アプリ

**トークン**:
あるアカウントが、ある OAuth クライアントに与えた認可。アカウントごとに 1 つだけ存在する。
_Avoid_: 認証情報、クレデンシャル、鍵

**サービスアカウント**:
人ではなくアプリケーションに属する Google アカウント。ブラウザでの認可を必要とせず、
アクセスできる範囲は「そのアドレスに共有されたファイル」だけで決まる。
_Avoid_: ボットアカウント、マシンアカウント、SA

**資格情報**:
アカウントが Google API を呼ぶために必要な情報の総称。OAuth クライアントとトークンの組か、
サービスアカウントの鍵のいずれか。どちらであるかはファイルの内容から判別する。
_Avoid_: 認証情報、クレデンシャル

**ホームディレクトリ**:
アカウント設定と全アカウントのトークンを収める場所。Docker ではここ 1 つをマウントする。
_Avoid_: データディレクトリ、設定ディレクトリ、ワークスペース

### ツールまわり

**ツール**:
AI アシスタントが呼び出せる操作の単位。MCP の tool に対応する。
_Avoid_: コマンド、API、アクション、関数

**コマンド**:
1 つのツールの実装。どのアカウントで動くかを知らず、解決済みの認証クライアントを渡されて実行する。
_Avoid_: ハンドラー、オペレーション

**サービス**:
1 つの Google Workspace プロダクト（Sheets / Docs / Slides / Drive）に属するコマンドの束。
_Avoid_: モジュール、プロバイダー

**メタツール**:
アカウントの解決を必要としない、サーバー自身の情報を返すツール。`accounts_list` が該当する。
_Avoid_: システムツール、内部ツール

### 書式まわり

**書式**:
セルや要素の見た目を決める値。中身（値・テキスト）とは独立して変更できる。
_Avoid_: スタイル、デザイン、フォーマット

**範囲**:
書式を適用するセルのたまり。ツール引数では常に A1 記法で渡し、sheetId や行列番号は外に見せない。
_Avoid_: レンジ、セル範囲、GridRange

### 表の操作まわり

**シート**:
スプレッドシートの中のタブ 1 枚。ツール引数ではシート名で指し、`sheetId` は外に見せない。
省略したときは先頭のシートを指す（削除だけは事故を避けるため省略を許さない）。
_Avoid_: タブ、ワークシート、ページ

**条件**:
値が当てはまるかどうかの判定。条件付き書式・入力規則・フィルタが同じ形を共有するが、
**使える種類はそれぞれ別の部分集合**で、用途違いは API に弾かれる。
_Avoid_: ルール、判定式、クライテリア

**入力規則**:
セルに入れてよい値の決まり。プルダウンもチェックボックスもこれの UI としての見え方であり、
別の機能ではない。設定と解除は別のツールに分けている。
_Avoid_: バリデーション、データ検証、制約

**フィルタ**:
シートに 1 つだけ置ける抽出の仕掛け。条件に合わない行を隠すだけで、**セルの中身は動かさない**。
_Avoid_: 絞り込み、抽出条件

**並べ替え**:
範囲の行を入れ替える操作。フィルタと違い中身が実際に動くので、見出し行を範囲に含めてはいけない。
_Avoid_: ソート、整列
